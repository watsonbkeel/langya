# 交接文档 — 2026-09-06 会话

> **给下一个 Codex 对话**：本文件记录了当前会话的完整状态、已完成和未完成的事项。

---

## 1. 当前状态速览

| 项目 | 状态 |
|---|---|
| 当前分支 | `main` |
| 最新 commit | `9eb4d82` — feat(mac): M4 场景视觉素材替换与交互物Billboard重构 |
| 仓库 | `github.com/watsonbkeel/langya`（PUBLIC） |
| 当前机器 | Mac（Darwin ARM64） |
| 工作区 | 干净，已全部提交 |
| 配置校验 | ✅ 通过 |
| 客户端类型检查 | ✅ 通过 |
| 服务端类型检查 | ✅ 通过 |
| Cocos 构建 | ✅ 完成（`client/build/web-mobile/`） |

---

## 2. 本次 commit 包含的内容（`9eb4d82`）

### 文件变更
- **31 files changed**, 950 insertions(+), 194 deletions(-)

### 核心改动

| 文件 | 改动内容 |
|---|---|
| `client/assets/scripts/level/m4-scene-decorations.ts` | 取消纯色台阶，改为岩土地面纹理 + 统一山地远景 + 沙袋机枪掩体 + 石垒/补给箱/枪架透明贴图；路线长度读取 `waves.json` |
| `client/assets/scripts/level/m3-world-interactions.ts` | 物资/武器架/重机枪从纯色方块Mesh改为Billboard贴图；加载真实九二式机枪素材 |
| `client/assets/scripts/core/billboard.ts` | 支持场景道具独立中心点(`centerY`)和横向比例(`widthScale`) |
| `client/assets/scripts/core/m1-game.ts` | 为场景装饰和世界交互物绑定摄像机更新 |
| `client/assets/scripts/player/first-person-controller.ts` | 鼠标上下方向平台覆盖修正 |
| `client/assets/scripts/ui/m1-hud.ts` | 结算页「重新开始新的一局」按钮文案 |
| `client/assets/scripts/config/game-config.ts` | 客户端路线配置增加 `lengthM` |
| `server/src/wave/wave-scheduler.ts` | 提前清空敌人可提前投放下一波 |
| `server/src/wave/wave-scheduler.test.ts` | 对应波次调度测试 |
| `docs/MILESTONES.md` | 回写进度 |
| `docs/OPEN-QUESTIONS.md` | 更新开放问题 |

### 新增素材
| 文件 | 用途 |
|---|---|
| `client/assets/resources/scene/rocky-ground.png` | 岩土地面纹理（不透明，水平铺设） |
| `client/assets/resources/scene/terrain-backdrop.png` | 近景地形背景 |
| `client/assets/resources/scene/mountain-backdrop.png` | 统一山地远景 |
| `client/assets/resources/scene/mg-emplacement.png` | 沙袋机枪掩体 |
| `client/assets/resources/scene/stone-barricade.png` | 石垒掩体 |
| `client/assets/resources/scene/supply-crate.png` | 补给箱 |
| `client/assets/resources/scene/weapon-rack.png` | 武器架 |
| `client/assets/resources/scene/terrain-panorama.png` | 地形全景（已清理白底） |

所有素材均已附带 `.meta` 文件，透明背景已逐张验证。

---

## 3. Debian 服务器当前状态

| 项目 | 状态 |
|---|---|
| Git HEAD | `9eb4d82`（已 pull） |
| 配置校验 | ✅ 通过 |
| 服务端构建 | ❌ **未执行**，需要 `cd server && npm run build` |
| 服务进程 | 运行中（PID 372723），但仍是旧版本 `c53856d` |
| Nginx | 正常运行，配置有效 |
| HTTP 本地 | 200 |
| HTTP Tailscale | 200 |

---

## 4. ⚠️ 未完成事项（下一步必须做）

### 优先级 P0：完成部署

```bash
# 1. Debian 构建服务端
ssh root@100.74.3.56 "cd /root/langya/langya/server && npm run build"

# 2. Mac 同步客户端构建产物到 Debian
rsync -avz --delete /Users/watson/Documents/微云盘/AI+小程序培训课件/长训营/狼牙山/client/build/web-mobile/ root@100.74.3.56:/var/www/langyashan/

# 3. 重启服务端
ssh root@100.74.3.56 "pkill -f 'node /root/langya/langya/server/dist/index.js' && cd /root/langya/langya/server && nohup node dist/index.js > /tmp/langya-server.log 2>&1 &"

# 4. 刷新 Nginx
ssh root@100.74.3.56 "nginx -s reload"

# 5. 验证
curl -I http://100.74.3.56:8080/
node tools/check-ws.js ws://100.74.3.56:8081/ws
```

### 优先级 P1：浏览器视觉验收

部署完成后，打开 `http://100.74.3.56:8080/` 验证：

1. 岩土地面是否显示为真实纹理（不是绿色方块）
2. 山地远景是否显示为单一正常画面（不是横向条纹）
3. 两个机枪位是否显示真实沙袋掩体
4. 重机枪模型是否不再是纯色方块
5. 石垒、枪架、补给箱是否没有白色背景
6. 三条路线是否仍然可辨识
7. 角色和道具是否没有漂浮、穿地或被远景遮挡
8. 浏览器控制台是否无资源加载错误

### 优先级 P2：遗留问题

用户在其他对话中提到的遗留问题：

1. **瞄准镜上下移动**：Mac 下方向反了，`first-person-controller.ts` 已修正，但需要在 Windows 上也验证
2. **队友 T-pose 造型**：全部是 T 字打开，需要多图生成（idle/run/fire 各多张变体）
3. **敌人素材**：看起来好一点，但也可考虑多图
4. **场景多图生成**：队友和场景都需要更多变体素材
5. **提前消灭敌人**：已实现，波次调度器支持提前投放下一波
6. **游戏结束**：需要「向英雄致敬」+「重新开始新的一局」按钮，按钮文案已改

---

## 5. 关键约束提醒

- **不要用子 Agents 并发**：用户明确要求只用主对话大模型，不要启动 Agents
- **AGENTS.md 铁律 3**：单人模式也走服务器，禁止写本地战斗逻辑
- **AGENTS.md 铁律 11**：双机协作，`server/**` 只有 Debian 改，`client/assets/scenes|resources|settings/**` 只有 Mac 改
- **仓库是 PUBLIC**：禁止提交任何 token、密码、API Key
- **生图接口**：`og-image2-low`，`sk-mrUCHn9G5zJ0bguMThQW4Ytj8v5rPeoSUlIH6kA5VbcQdOai`，URL `https://api.bkeel.com/v1`，异步，~30s/张，并发 5，100 张内直接用，超过需确认
- **素材透明背景**：生图 prompt 必须明确要求透明背景，生成后逐张验证

---

## 6. 文件路径速查

| 用途 | 路径 |
|---|---|
| 项目根目录 | `/Users/watson/Documents/微云盘/AI+小程序培训课件/长训营/狼牙山` |
| 需求文档 | `docs/PRD.md`（v1.2） |
| 协作纪律 | `docs/COLLAB.md` |
| 最高约束 | `AGENTS.md` |
| 里程碑 | `docs/MILESTONES.md` |
| 开放问题 | `docs/OPEN-QUESTIONS.md` |
| 客户端源码 | `client/assets/scripts/` |
| 客户端素材 | `client/assets/resources/` |
| 客户端构建产物 | `client/build/web-mobile/` |
| 服务端源码 | `server/src/` |
| 共享配置 | `shared/config/*.json` |
| 素材加工脚本 | `tools/asset-pipeline/` |
| Debian 仓库目录 | `/root/langya/langya/` |
| Debian Nginx 目录 | `/var/www/langyashan/` |
| 测试地址 | `http://100.74.3.56:8080/` |
| WS 测试地址 | `ws://100.74.3.56:8081/ws` |

---

## 7. 用户原始需求（完整引用）

> 检查这个项目进度推进情况。目前由于多Agents并发出现大量超时超出容量的问题，所以帮我把这个文档改为仅使用一个主对话大模型持续开发，不要轻易启动Agents去并发处理。
>
> 我在另外一个对话还有一些遗留问题：瞄准镜的上下移动在mac下好像是反的，但是我不确定windows是否正确。另外，队友，全是T字打开的造型，敌人看起来好一点，现在需要也给队友和场景也做多图生成。如果提前消灭全部敌人，可以提前投放下一批敌人。游戏结束，就是给一个像英雄致敬，应该再给一个重新开始新的一局。
>
> 生图大模型接口：og-image2-low, api.bkeel.com/v1，异步，30秒/张，并发5，100张内直接用，超过需确认
>
> 立即修改
>
> 请先把我这次在工作区里做的改动部署到服务器，再让我测试。
> 当前要部署的内容包括：鼠标上下方向的修正/平台覆盖、提前清空敌人后可提前投放下一波、结算页「重新开始新的一局」按钮文案、新生成并回填的队友素材、新生成并接入的场景素材（远景山地、补给箱）、相关的客户端脚本、波次调度、文档更新
>
> 按这个顺序做：1. 先检查当前工作区和分支状态 2. 先执行 git pull --rebase origin main 3. 跑 node tools/verify-config.js 4. 完成必要的类型检查和构建 5. 把最新客户端产物部署到 Debian 服务器 6. 刷新 Nginx / PM2 7. 用正式 URL 实际验证新版本 8. 告诉我新的测试地址、部署结果和我该怎么体验
>
> 要求：不要用子 Agents 并发、不要跳过验证、不要只告诉我"本地改好了"，必须真的部署到服务器、如果部署过程中有阻塞，直接说明卡在哪一步

**当前完成度**：步骤 1-4 已完成，步骤 5-8 尚未执行。

---

*文档生成时间：2026-09-06 23:42 CST*
*当前机器：Mac (watsonmacbook-air)*
