# 交接文档 — 2026-09-06 会话（修订版）

> **给下一个 Codex 对话**：本文是当前阶段的主交接入口，记录当前状态、目标、执行顺序、开发范围和验收标准。请先读 §0–§1，再严格按 §6→§7 执行。
> 本文件已由 deepseek 初稿、经小B 修订。若是新 clone，必须先确认本文件及 §11 列出的阶段纪律文档已经存在于当前 commit；即使阶段纪律文档暂时不存在，也可按 §9 的单线规则继续，不要直接启动并发 Agents。

---

## 0. 🔒 安全红线（必读，违反即事故）

- **仓库是 PUBLIC**（`github.com/watsonbkeel/langya`），严禁任何 token / 密码 / API Key 入库。`.gitignore` 已覆盖 `.env`、`*.key` 等，但**文档 `.md` 同样受控**，写明文即泄漏。
- **生图 API Key 已泄漏**：原 Key 于 commit `fabb254`（即提交本文档自身的那次）被明文写入并推送至公开仓库，当前已公开可查。**使用前必须先到 bkeel 后台轮换 Key**，否则等同于使用公开凭据。
- 本文档中生图 Key 一律用占位符 `<BKEEL_IMAGE_API_KEY>`；现有脚本从环境变量 `BKEEL_IMAGE_API_KEY` 读取，不写入文件（`.env` 已被 gitignore）。**提交前确认本文档不含任何明文凭据**。

---

## 1. 当前目标与完成判据

### 当前阶段目标

本次交接的短期目标是：**先把 commit `9eb4d82` 的 M4 改动部署到 Debian 并完成浏览器验收，再在不使用并发 Agents 的前提下，按 D1→D4 顺序收口 M4 视觉与体验问题。**

完成本次交接任务，必须满足：

- 当前客户端和服务端已部署到 Debian，HTTP 与 WebSocket 均验证通过；
- §7 的 P1 浏览器验收 8 项全部通过，或逐项记录未通过原因；
- D1–D4 中已实施的需求均有代码 / 素材变更、构建结果和浏览器实测记录；
- D5–D6 已完成行为验收；
- 每个阶段性目标均更新 `docs/MILESTONES.md` 和必要的 `docs/OPEN-QUESTIONS.md`；
- 不把“代码已修改”“构建通过”写成“功能已完成”，除非有实际运行验收。

### M4 → M5 → M6 的长期目标

- **M4：**完成角色、场景、武器、HUD 等视觉与表现层收口，达到可展示、可验收状态；
- **M5：**完成创建 / 加入房间、五席真人 / AI 顶替、状态同步、断线重连、多人结算等联机闭环；
- **M6：**完成性能、首屏、资源加载、网络异常恢复、结算体验和发布准备。

### 完整推进顺序

1. **阶段 A：部署与基线验收**——完成 §7 P0、P1，不改新功能；
2. **阶段 B：收口 M4**——依次完成 D1→D2→D3→D4，并验收 D5、D6；
3. **阶段 C：推进 M5**——按 `docs/M4-M6多Agents.md` 与 PRD 完成联机闭环；
4. **阶段 D：收口 M6**——完成性能、异常恢复、体验打磨和发布文档；
5. **阶段 E：最终交付**——全量构建、部署、正式 URL 实测、回写里程碑并给出体验说明。

新对话完成阶段 A 后不要自动结束，应继续阶段 B；阶段 B 完成后继续 M5/M6。只有遇到以下情况才停下来询问 watson：

- 需要花钱或生图总量预计超过 100 张；
- 需要删除、覆盖不可恢复数据、重写 Git 历史或 force push；
- 产品需求出现相互冲突且无法按文档优先级判断；
- 需要 Windows 实机、外部账号或缺失凭据，当前环境无法提供。

M5/M6 的详细推进纪律以 `docs/M4-M6多Agents.md` 为准；本 HANDOFF 负责提供当前接手状态和本轮具体任务，不替代产品需求文档。

---

## 2. 接手前置检查与执行边界

新对话开始后先执行：

```bash
cd "/Users/watson/Documents/微云盘/AI+小程序培训课件/长训营/狼牙山"
uname -a
git status --short --branch
git fetch origin
git rev-parse HEAD origin/main
node tools/verify-config.js
```

执行规则：

- 当前本地 HANDOFF 修订会造成工作区非干净；另有既存未跟踪文档、素材和输出目录。**不得用 `reset --hard`、`clean -fd` 或批量删除来恢复干净状态。**
- 只有在工作区允许安全 rebase 时才执行 `git pull --rebase origin main`；若 `HEAD` 与 `origin/main` 相同，则无需为了形式强行 pull。
- 编码前必须读取 `AGENTS.md`、`docs/PRD.md`、`docs/COLLAB.md`、`docs/MILESTONES.md`、`docs/OPEN-QUESTIONS.md`、`docs/DEPLOY.md`。
- 本次短期任务以本 HANDOFF 的 D1–D6 为准；若与 `AGENTS.md` 或 PRD 冲突，按 `AGENTS.md` → PRD → COLLAB → 本 HANDOFF 的顺序执行并记录冲突。
- 新对话若运行在**当前同一工作区**，可直接读取本修订版；若是重新 clone，只有在本修订版被 commit 并 push 后才能获得。未确认前不要误以为远端已有最新版。

---

## 3. 当前状态速览

| 项目 | 状态 |
|---|---|
| 当前分支 | `main` |
| 实际 HEAD | `fabb254` — docs(mac): 2026-09-06 会话交接文档（已推送 `origin/main`） |
| 最近功能 commit | `9eb4d82` — feat(mac): M4 场景视觉素材替换与交互物Billboard重构 |
| 仓库 | `github.com/watsonbkeel/langya`（PUBLIC） |
| 当前机器 | Mac（Darwin ARM64） |
| 工作区状态（交接时快照） | 干净，`fabb254` 已全部提交；本次修订会使工作区出现本文件的未提交改动 |
| 配置校验 | ✅ 通过 |
| 客户端类型检查 | ✅ 通过 |
| 服务端类型检查 | ✅ 通过 |
| Cocos 构建 | ✅ 完成（`client/build/web-mobile/`） |

> ⚠️ 注：本文初稿自述"最新 commit `9eb4d82`"，但 `fabb254`（提交本文档自身）已取代它成为 HEAD。接手者以 `git log` 实际 HEAD 为准。

---

## 4. 本次会话已完成的改动

### 核心改动（commit `9eb4d82`，31 files changed, 950+/194-）

| 文件 | 改动内容 |
|---|---|
| `client/assets/scripts/level/m4-scene-decorations.ts` | 取消纯色台阶，改为岩土地面纹理 + 统一山地远景 + 沙袋机枪掩体 + 石垒/补给箱/枪架透明贴图；路线长度读取 `waves.json` |
| `client/assets/scripts/level/m3-world-interactions.ts` | 物资/武器架/重机枪从纯色方块Mesh改为Billboard贴图；加载真实九二式机枪素材 |
| `client/assets/scripts/core/billboard.ts` | 支持场景道具独立中心点(`centerY`)和横向比例(`widthScale`) |
| `client/assets/scripts/core/m1-game.ts` | 为场景装饰和世界交互物绑定摄像机更新 |
| `client/assets/scripts/player/first-person-controller.ts` | 鼠标上下方向平台覆盖修正（mac 端；win 端未验证，见 §10） |
| `client/assets/scripts/ui/m1-hud.ts` | 结算页「重新开始新的一局」按钮文案 |
| `client/assets/scripts/config/game-config.ts` | 客户端路线配置增加 `lengthM` |
| `server/src/wave/wave-scheduler.ts` | 提前清空敌人可提前投放下一波 |
| `server/src/wave/wave-scheduler.test.ts` | 对应波次调度测试 |
| `docs/MILESTONES.md` / `docs/OPEN-QUESTIONS.md` | 回写进度与开放问题 |

### 新增素材（均附 `.meta`，透明背景已逐张验证）
`rocky-ground.png`、`terrain-backdrop.png`、`mountain-backdrop.png`、`mg-emplacement.png`、`stone-barricade.png`、`supply-crate.png`、`weapon-rack.png`、`terrain-panorama.png`（已清理白底）。

---

## 5. Debian 服务器当前状态（部署前必读）

| 项目 | 状态 |
|---|---|
| Git HEAD | `fabb254`（已 pull） |
| 配置校验 | ✅ 通过 |
| 服务端构建 | ❌ **未执行**，需 `cd server && npm run build` |
| 服务进程 | 运行中，但仍是旧功能版本 `c53856d`；原记录的 PID 可能已变化，接手时用 PM2 重新查询 |
| Nginx | 正常运行，配置有效 |
| HTTP 本地 | 200 |
| HTTP Tailscale | 200 |

> 服务器 IP `100.74.3.56` 为**当前值**，若重启换 IP 即失效；以 frpc / Tailscale 映射为准。

---

## 6. ⚠️ 接手者第一动作：先部署，再开发

**强约束**：接手者**第一步 = 按 §7 P0 部署当前 commit，并按 §7 P1 八条进行浏览器验收**；确认无误后，再按 `docs/M4-M6多Agents.md` 的单线纪律推进新需求。**禁止在未部署验证前直接改新代码。**

阶段纪律主文档为 `docs/M4-M6多Agents.md`（详见 §11 文档导航）。两份文档关系：HANDOFF = 当前状态 / 部署 / 具体待办；M4-M6 = 阶段纪律 / 推进路线。若新对话暂时看不到该本地未跟踪文件，至少严格执行 §9 已内嵌的同等规则，不得因此恢复并发。

---

## 7. 未完成事项

### 优先级 P0：完成部署

```bash
# 1. Mac 端确认客户端构建产物存在
cd "/Users/watson/Documents/微云盘/AI+小程序培训课件/长训营/狼牙山"
test -f client/build/web-mobile/index.html

# 2. 同步 Mac 客户端构建产物到 Debian 仓库的标准构建目录
rsync -avz --delete client/build/web-mobile/ root@100.74.3.56:/root/langya/langya/client/build/web-mobile/

# 3. Debian 更新源码并走项目正式部署脚本
ssh root@100.74.3.56 'cd /root/langya/langya && git pull --rebase origin main && tools/deploy.sh'

# 4. 从 Mac 验证 Tailscale HTTP、直连 WS 和 Nginx 同源 WS
curl -fsSI http://100.74.3.56:8080/
node tools/check-ws.js ws://100.74.3.56:8081/ws
node tools/check-ws.js ws://100.74.3.56:8080/ws
node tools/check-m1-ws.js ws://100.74.3.56:8080/ws
node tools/check-m2-ws.js ws://100.74.3.56:8080/ws
node tools/check-m3-ws.js ws://100.74.3.56:8080/ws
```

> `tools/deploy.sh` 是正式入口，会执行配置校验、服务端依赖安装、类型检查、测试、构建、静态文件发布、Nginx 校验、PM2 reload 以及 HTTP/WS 联调。**不要再使用 `pkill + nohup` 绕过 PM2。**若脚本失败，保留输出并明确报告卡在哪一步。

### 优先级 P1：浏览器视觉验收

部署完成后，打开 `http://100.74.3.56:8080/` 验证：

1. 岩土地面是否显示为真实纹理（不是绿色方块）
2. 山地远景是否显示为单一正常画面（不是横向条纹）
3. 两个机枪位是否显示真实沙袋掩体
4. 重机枪模型是否不再是纯色方块
5. 石垒、枪架、补给箱是否没有白色背景
6. 三条路线是否仍然可辨识
7. 角色和道具是否没有漂浮、穿地或被远景遮挡
8. 浏览器控制台是否无资源加载错误

### 优先级 P2：后续开发需求清单（带验收标准）

> 以下为从用户原始需求转化出的**可执行任务**，每条含验收标准。生图类需求统一遵守 §8 总量与 §9 接口规则，透明背景逐张验证。

### 本轮角色素材范围（已锁定，不再猜测）

- **五名壮士必须分别生成自己的角色素材**：马宝玉、葛振林、宋学义、胡德林、胡福才；不能只生成一套公共队友图。
- 每名角色第一阶段至少生成 `idle`、`run`、`fire` 三态，共 **5 × 3 = 15 张基础角色图**；全身、自然战斗姿态、透明背景、无 T-pose。
- 素材目录建议采用稳定席位路径：`client/assets/resources/chars/heroes/seat-0..4/{idle,run,fire}.png`，席位与 `shared/config/allies.json` 的 `heroNames` 顺序一致。
- 当前 `AllyRenderer` 仅按一套 `allySpritePath` 加载三态素材，因此 D1 不只是生图：还必须改造 `client/assets/scripts/ally/ally-renderer.ts`，按 `AllyState.heroName` 或 `seatIndex` 为每名角色选择各自贴图并缓存材质。
- 当前 `tools/asset-pipeline/clean-generated.py` 只识别 `cn-soldier/jp-soldier` 命名并回填公共目录；D1 必须同步扩展该脚本（或新增等价、可复跑的处理脚本），将五人素材清理为 512×512 RGBA 并回填各自目录。
- `tools/asset-pipeline/verify-output.py` 只会强校验配置中声明的素材；因此五人素材路径必须写入 `shared/config/allies.json`（并同步客户端配置），不能只把 PNG 扔进目录而不声明。
- 如果后续需要 2–4 帧连续动作，再作为第二阶段扩展；本轮先完成五人差异化的三态静态图与正确切换。

| 编号 | 需求 | 优先级 | 涉及文件 / 目录 | 验收标准 | 备注 |
|---|---|---|---|---|---|
| D1 | 五名壮士分别生成并接入角色图 | P1 | `shared/config/allies.json`、`client/assets/resources/chars/heroes/`、`client/assets/scripts/ally/ally-renderer.ts` | 五人各有 idle/run/fire 三态，共 15 张；无 T-pose；透明背景；游戏中 4 名可见 AI 队友按姓名/席位显示不同角色，不再共用同一张图；状态切换正确 | 马宝玉也必须生成，为后续多人模式和视角切换预留；当前单人第一人称时本人可不显示 |
| D2 | 瞄准镜上下方向修正 | P1 | `client/assets/scripts/player/first-person-controller.ts` | mac 实机验证方向正确；Windows 代码路径和测试步骤写清，最终实机验收由有 Windows 环境的一方完成 | Windows 未实测前标记“待 Windows 验收”，不能阻塞其余 M4 开发，也不能虚报已完成 |
| D3 | 敌人现有三态素材复核 | P2 | `client/assets/resources/chars/jp-soldier/`、敌人渲染器 | idle/run/fire 切换正常、透明背景、无明显 T-pose 或漂浮；若现有素材已达标，不强制继续生图 | 用户原意是敌人“看起来好一点”，本轮重点是队友和场景，避免无效扩量 |
| D4 | 场景多图生成与接入 | P2 | `client/assets/resources/scene/`、`client/assets/scripts/level/m4-scene-decorations.ts` | 远景山地、地形、机枪位、石垒、补给箱、枪架等按不同用途使用独立素材；无白底、无拉伸条纹、接地正确、三条路线可辨识 | 不以“生成了图片”为完成，必须接入场景并浏览器验收 |
| D5 | 游戏结束页 | 已完成·待验收 | `client/assets/scripts/ui/m1-hud.ts` | 显示「向英雄致敬」并提供「重新开始新的一局」按钮；按钮能真正重开一局 | 文案已改，功能仍需浏览器验收 |
| D6 | 提前清敌投下波 | 已完成·待验收 | `server/src/wave/wave-scheduler.ts` | 当前波全部消灭后，无需等待原定时间即可投放下一波；不能重复投放或打乱总波数 | 已实现，需浏览器实测验收 |

### 已完成但待验收（明确区分，避免重复劳动）

- D5 游戏结束按钮文案（已改，待验收）
- D6 提前清敌投下波（已实现，待验收）
- M4 场景视觉素材替换 + 交互物 Billboard 重构（commit `9eb4d82`，待部署验收）

---

## 8. 生图总量预估（决定是否需先确认）

基于已锁定的范围估算：

| 类别 | 预估张数 |
|---|---|
| 五名壮士基础三态（D1） | **15 张**（5 人 × idle/run/fire） |
| D1 失败重试 / 备选 | 约 5–15 张 |
| 敌人素材（D3） | 默认复用现有素材；复核不通过时补 3–6 张 |
| 场景变体（D4） | 约 10–15 张 |
| **预计合计** | **约 30–50 张** |

合计预计未超 100 张，可在 Key 轮换后直接生成。**本轮不以一套公共队友素材代替五名角色。**若第二阶段要给每名角色的每种动作生成 2–4 帧连续动画，需重新核算总量；超过 100 张必须先向 watson 确认。

### 生图执行方式（Key 轮换后）

```bash
cd "/Users/watson/Documents/微云盘/AI+小程序培训课件/长训营/狼牙山"
export BKEEL_IMAGE_API_KEY='<轮换后的新 Key，仅当前 shell 使用>'
python3 tools/asset-pipeline/image-agent.py <五人及场景任务清单.json> \
  --output-dir <临时下载目录> \
  --base-url https://api.bkeel.com/v1 \
  --model og-image2-low \
  --max-concurrency 5
```

随后依次：运行已扩展的透明背景清理/回填脚本 → `python3 tools/asset-pipeline/verify-output.py` → `node tools/verify-config.js` → 客户端类型检查与 Cocos 构建 → 部署浏览器验收。Key 只通过当前 shell 环境变量提供，不写入 manifest、文档、日志或仓库。

---

## 9. 关键约束提醒

- **单线执行规则（即使看不到 M4-M6 文档也必须执行）**：一次只做一个最小目标；修改后立即验证；记录结果后再进入下一项；不要并行改同一批文件；不得用并发 Agents。
- **任务顺序**：部署与 P1 验收 → D1 五名壮士独立素材与渲染器改造 → D2 输入方向 → D3 敌人素材复核 → D4 场景多图 → D5/D6 行为验收 → 回写文档。
- **AGENTS.md 铁律 3**：单人模式也走服务器，禁止写本地战斗逻辑。
- **AGENTS.md 铁律 11**：双机协作，`server/**` 只有 Debian 改，`client/assets/scenes|resources|settings/**` 只有 Mac 改。
- **仓库 PUBLIC**：禁止提交任何 token、密码、API Key（见 §0）。
- **生图接口**：仓库脚本 `tools/asset-pipeline/image-agent.py` 实际读取环境变量 `BKEEL_IMAGE_API_KEY`，默认模型 `og-image2-low`，默认 Base URL `https://token.bkeel.com/v1`；若本轮必须改用用户指定的 `https://api.bkeel.com/v1`，通过 `--base-url https://api.bkeel.com/v1` 显式覆盖，不要改错环境变量名。异步，约 30 秒/张，并发上限 5，单批超过 100 张需先确认。
- **素材透明背景**：生图 prompt 必须明确要求透明背景，生成后逐张验证。

---

## 10. 未验证项 / 风险点

- **瞄准镜 win 端方向未验证**：mac 已修，win 未知（见 D2）。
- **提前清敌仅代码实现**：需浏览器实测确认行为符合预期（见 D6）。
- **T-pose 多图后性能 / FPS 未知**：大量 Billboard 变体可能影响首屏与帧率，验收时关注。
- **服务器 IP 可能变动**：`100.74.3.56` 非固定，以 frpc / Tailscale 映射为准。
- **两份 M4-M6 文档当前未纳入 Git 跟踪**：新 clone 可能看不到；本 HANDOFF 已把单线开发硬规则内嵌在 §9，因此可继续当前短期任务。提交交接包时应把有效主文档 `docs/M4-M6多Agents.md` 一并纳入版本管理。

---

## 11. 文档导航（避免看错文档）

本仓库 `docs/` 下指导类文档较多，**接手者以以下为准**：

| 用途 | 文档 | 说明 |
|---|---|---|
| 最高约束 | `AGENTS.md` | 所有规则以它为准 |
| 需求 | `docs/PRD.md`（v1.2，1061 行） | 完整产品需求 |
| 协作纪律 | `docs/COLLAB.md` | |
| 阶段纪律（单线推进） | **`docs/M4-M6多Agents.md`** | **有效主文档**（本地未跟踪）；文件名含"多Agents"但内容是**禁用并发**，以内容为准 |
| 一次性推进指令 | `docs/M4-M6推进.md` | 更早的推进 prompt，纪律一致但偏一次性，以 M4-M6多Agents.md 为准 |
| 进度 / 开放问题 | `docs/MILESTONES.md`、`docs/OPEN-QUESTIONS.md` | |
| 部署 | `docs/DEPLOY.md` | |

> ⚠️ `docs/HANDOFF-M3-PLAYTEST.md`、`docs/CODEX-PROMPTS.md`、`docs/MODEL-ROUTING.md`、`docs/DEVELOPMENT-PLAN-AFTER-VISUAL-REVIEW.md` 等为历史 / 专项文档，非当前接手必读，按需查阅。

---

## 12. 文件路径速查

| 用途 | 路径 |
|---|---|
| 项目根目录 | `/Users/watson/Documents/微云盘/AI+小程序培训课件/长训营/狼牙山` |
| 需求文档 | `docs/PRD.md`（v1.2） |
| 协作纪律 | `docs/COLLAB.md` |
| 最高约束 | `AGENTS.md` |
| 阶段纪律（单线） | `docs/M4-M6多Agents.md` |
| 里程碑 | `docs/MILESTONES.md` |
| 开放问题 | `docs/OPEN-QUESTIONS.md` |
| 客户端源码 | `client/assets/scripts/` |
| 客户端素材 | `client/assets/resources/`（现有公共角色：`chars/cn-soldier`、`chars/jp-soldier`；D1 新增五人角色：`chars/heroes/seat-0..4/`） |
| 客户端构建产物 | `client/build/web-mobile/` |
| 服务端源码 | `server/src/` |
| 共享配置 | `shared/config/*.json` |
| 素材加工脚本 | `tools/asset-pipeline/` |
| Debian 仓库目录 | `/root/langya/langya/` |
| Debian Nginx 目录 | `/var/www/langyashan/` |
| 测试地址 | `http://100.74.3.56:8080/`（IP 可能变动） |
| WS 测试地址 | `ws://100.74.3.56:8081/ws` |

---

## 13. 用户原始需求（摘要 + 完整引用）

**摘要**：禁用并发 Agents，只用主对话；修复瞄准镜方向、补队友与场景多图、实现提前清敌与游戏结束按钮；**先把本次改动部署到服务器让我测试**。

**完整引用（保留上下文）**：

> 检查这个项目进度推进情况。目前由于多Agents并发出现大量超时超出容量的问题，所以帮我把这个文档改为仅使用一个主对话大模型持续开发，不要轻易启动Agents去并发处理。
>
> 我在另外一个对话还有一些遗留问题：瞄准镜的上下移动在mac下好像是反的，但是我不确定windows是否正确。另外，队友，全是T字打开的造型，敌人看起来好一点，现在需要也给队友和场景也做多图生成。如果提前消灭全部敌人，可以提前投放下一批敌人。游戏结束，就是给一个像英雄致敬，应该再给一个重新开始新的一局。
>
> 生图大模型接口：og-image2-low, api.bkeel.com/v1，异步，30秒/张，并发5，100张内直接用，超过需确认
>
> 立即修改
>
> 请先把我这次在工作区里做的改动部署到服务器，再让我测试。
> 当前要部署的内容包括：鼠标上下方向的修正/平台覆盖、提前清空敌人后可提前投放下一波、结算页「重新开始新的一局」按钮文案、新生成并回填的队友素材、新生成并接入的场景素材（远景山地、补给箱）、相关的客户端脚本、波次调度、文档更新
>
> 按这个顺序做：1. 先检查当前工作区和分支状态 2. 先执行 git pull --rebase origin main 3. 跑 node tools/verify-config.js 4. 完成必要的类型检查和构建 5. 把最新客户端产物部署到 Debian 服务器 6. 刷新 Nginx / PM2 7. 用正式 URL 实际验证新版本 8. 告诉我新的测试地址、部署结果和我该怎么体验
>
> 要求：不要用子 Agents 并发、不要跳过验证、不要只告诉我"本地改好了"，必须真的部署到服务器、如果部署过程中有阻塞，直接说明卡在哪一步

**当前完成度**：步骤 1-4 已完成，步骤 5-8（部署与验收）尚未执行；D1–D6 开发需求见 §7 表格。

---

*文档初稿：deepseek（2026-09-06 23:42 CST）｜最终修订：小B（2026-09-07，补充目标与完成判据、五名壮士独立三态素材方案、渲染器改造要求、正式 PM2 部署流程、真实生图脚本参数、单线执行规则与交接可见性说明）*
