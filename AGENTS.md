# AGENTS.md — 狼牙山五壮士项目开发约束

> **本文件是最高约束。任何与本文件冲突的判断，以本文件为准。**
> 开始任何编码前必须完整读完本文件，以及 `docs/PRD.md`。

---

## 0. 三十秒速览

| 项 | 内容 |
|---|---|
| 项目 | 狼牙山五壮士 · 2.5D 网页固守射击游戏 |
| 代号 | `langyashan-defense` |
| 引擎 | Cocos Creator 3.8 LTS（Web-Mobile 构建） |
| 服务端 | Node.js 22 + TypeScript + ws（权威服务器） |
| v1.0 形态 | **单人模式**，1 真人 + 4 名 AI 队友 |
| 敌人 | 日军 200 人，四波 30 / 50 / 60 / 60 |
| 单局 | 5 分钟，存活即胜利 |
| 部署 | Debian 工作站，PM2 + Nginx，内网可达即可 |
| 需求文档 | `docs/PRD.md`（v1.1，唯一需求真源） |

---

## 1. 十条铁律（违反即返工）

### 铁律 1：需求以 PRD 为准，不要自行发挥

`docs/PRD.md` 是唯一需求真源。**不要**因为「我觉得这样更好玩」而改动玩法、数值或流程。

有更好的想法 → 写进 `docs/OPEN-QUESTIONS.md` 的「Codex 追加区」，**同时按 PRD 原样实现**。

### 铁律 2：数值必须来自配置文件，禁止硬编码

所有数值（伤害、血量、射速、波次、时间、概率）必须从 `shared/config/*.json` 读取。

```ts
// ❌ 禁止
const RIFLE_DAMAGE = 55;
if (enemy.hp <= 0) { ... }
setTimeout(spawnWave, 12000);

// ✅ 正确
import { weapons } from '@shared/config';
const damage = weapons.liaoshi13.damage;
```

**判据**：全局搜索代码中的裸数字，除了 0、1、-1、数组索引和纯数学常量，不应有业务数值。

### 铁律 3：单人模式也走服务器，禁止写本地战斗逻辑

这是**最容易做错、且返工成本最高**的一条。

```
❌ 错误理解：「先做单人」= 客户端本地跑战斗逻辑
✅ 正确做法：「先做单人」= 服务器房间里 1 个真人 + 4 个 AI 队友
```

单人模式同样：连 WebSocket、走完整协议、由服务器裁决伤害。**客户端永远不做伤害计算。**

理由：M5 做联机时，战斗逻辑零改动即可复用。若写了本地版本，等于战斗系统重做一遍。

### 铁律 4：客户端不做任何判定

| 客户端职责 | 服务端职责 |
|---|---|
| 渲染、输入采集、音效、UI、特效表现 | 伤害计算、命中裁决、AI 决策、胜负判定、计分 |

客户端上报「我朝这个方向开了一枪」，**不是**「我打死了 3 号敌人」。

### 铁律 5：原始素材目录只读

```
军服素材/    ← 只读，禁止修改、移动、删除、重命名
武器素材/    ← 只读，同上
```

加工产物一律输出到 `client/assets/resources/`。加工脚本必须是**读取原目录 → 写入新目录**，绝不原地修改。

### 铁律 6：不碰 frpc、不碰域名、不碰对外暴露

部署交付边界**到内网为止**：

- ✅ 服务在 Debian 上跑起来、PM2 常驻、开机自启
- ✅ 局域网地址和 Tailscale 地址都能访问
- ❌ 不配置 frpc
- ❌ 不申请/配置域名
- ❌ 不配置对外反向代理和证书

对外转发由 watson 自行处理。

### 铁律 7：场景用代码生成，不依赖编辑器手工摆放

开发环境是**无 GUI 的 Debian**，Cocos Creator 编辑器打不开。

```
.scene 文件里只允许有：根节点 + 摄像机 + 一个挂载入口脚本的节点
其余一切（地形、掩体、枪位、UI）由 TypeScript 代码动态创建
```

**判据**：删掉 `.scene` 里除入口外的所有节点，游戏仍能正常运行。

### 铁律 8：协议类型定义只有一份

`shared/protocol.ts` 是唯一真源，客户端和服务端都从这里导入。

```ts
// ❌ 禁止在 client/ 和 server/ 各写一份 interface
// ✅ 两边都 import from '@shared/protocol'
```

### 铁律 9：适龄合规是硬红线

| 禁止 | 说明 |
|---|---|
| 血液、血迹、残肢 | 命中反馈用尘土粒子 + 准星闪烁 |
| 血腥音效 | 用闷响代替 |
| 仇恨符号、侮辱性称谓 | 敌方统一称「日军」，不用任何蔑称 |
| 付费、抽卡、外部跳转 | 一律不做 |

目标用户是 9–15 岁青少年，这条没有商量余地。

### 铁律 10：高风险操作必须先问

以下操作**停下来问 watson**，不要自作主张：

- 删除任何已存在的文件或目录
- 修改 Debian 上与本项目无关的服务、配置、端口
- 安装会影响全局的软件包
- 对外发布任何链接
- 变更技术选型（引擎、网络模型、数据库）

---

## 2. 项目结构

```
狼牙山/
├── AGENTS.md                    ← 本文件，最高约束
├── README.md                    ← 项目入口说明
├── docs/
│   ├── PRD.md                   ← 需求唯一真源（v1.1）
│   ├── OPEN-QUESTIONS.md        ← 开放问题 + Codex 追加区
│   ├── DEPLOY.md                ← 部署说明（Codex 完成后补充）
│   └── MILESTONES.md            ← 里程碑进度跟踪
├── skills/                      ← 分域开发规范
│   ├── asset-pipeline/SKILL.md  ← 素材二次加工
│   ├── server-authority/SKILL.md← 权威服务器架构
│   ├── ai-behavior/SKILL.md     ← 敌人与队友 AI
│   ├── cocos-codegen/SKILL.md   ← 代码生成场景
│   └── deployment/SKILL.md      ← 部署运维
├── shared/
│   ├── protocol.ts              ← 协议唯一真源（待建）
│   └── config/*.json            ← 数值唯一真源（已就绪）
│       gameplay / weapons / enemies / allies / waves
├── client/                      ← Cocos Creator 工程
├── server/                      ← Node.js 权威服务器
├── tools/                       ← 加工脚本、自测脚本
│   ├── verify-config.js         ← 配置校验（改配置后必跑）
│   ├── check-ws.js              ← WS 连通性自测
│   ├── simulate-match.js        ← AI 数值校准（M2 需实现）
│   └── asset-pipeline/          ← 素材加工（M4）
├── .env.example                 ← 环境变量模板（.env 不入库）
├── 军服素材/                     ← 只读
└── 武器素材/                     ← 只读
```

**已就绪、可直接用的东西**（不要重复造）：

| 文件 | 说明 |
|---|---|
| `shared/config/*.json` | 全部数值已按 PRD 填好，直接读取 |
| `tools/verify-config.js` | 配置校验逻辑已实现，服务端启动校验直接复用同等规则 |
| `tools/check-ws.js` | WS 自测脚本可直接跑 |
| `tools/asset-pipeline/*.py` | 抠图裁切脚本可直接跑（需 pillow+numpy） |

**需要你实现/补齐的**：`shared/protocol.ts`、`client/`、`server/`、`tools/simulate-match.js`、`tools/deploy.sh`、`docs/DEPLOY.md` 的待填写项。

---

## 3. 开工顺序

**严格按里程碑推进，不要跳步。** 详见 `docs/MILESTONES.md`。

| 阶段 | 目标 | 完成判据 |
|---|---|---|
| M0 | 骨架 + 连通 + 部署跑通 | 浏览器连上服务器收到心跳，Tailscale 地址可访问 |
| M1 | 战斗核心 | 能打死一个敌人，手感成立 |
| M2 | AI 队友 | 4 名队友分守路线，实测歼敌占比 ≤ 50% |
| M3 | 完整单局 | **单人打完 5 分钟并看到战报 → 交 watson 试玩** |
| M4 | 美术接入 | 画面可展示 |
| M5 | 联机 | 多人同房，成绩正确 |
| M6 | 打磨上线 | 通过 PRD 第 12 章全部验收项 |

**M3 是最重要的节点**：单人不好玩，加美术和联机都救不回来。M3 完成必须停下来交付试玩。

**M4 之前用色块占位**，不要因为等美术资源阻塞玩法开发。

---

## 4. 编码规范

### 语言与风格

- TypeScript 严格模式（`strict: true`）
- 变量、函数名用英文；**注释用中文**（本项目也是教学案例，可读性优先）
- 关键算法（AI 决策、命中判定、波次调度）必须有中文注释说明思路

### 命名

| 类型 | 规范 | 示例 |
|---|---|---|
| 文件 | kebab-case | `enemy-spawner.ts` |
| 类 | PascalCase | `WaveScheduler` |
| 函数/变量 | camelCase | `calculateDamage` |
| 常量 | UPPER_SNAKE | `MAX_ALIVE_ENEMIES` |
| 配置键 | camelCase | `spawnInterval` |

**目录名一律英文**。素材加工时 `武器素材/手榴弹/` → `weapons/grenade/`。

### 模块边界

```
server/src/
├── room/      席位管理、AI 队友补位 —— 不含战斗逻辑
├── game/      战斗主循环 20Hz —— 调度者，不含具体规则
├── ai/
│   ├── enemy/ 日军 AI
│   └── ally/  队友 AI
├── combat/    伤害与命中 —— 纯函数优先，便于单测
├── wave/      波次与空投调度
├── score/     计分与 MVP
└── db/        SQLite 战报
```

**combat 层写成纯函数**：输入状态，输出结果，不依赖全局。这样能直接写单元测试验证数值。

### 禁止事项

- 禁止 `any`（确实需要时用 `unknown` + 类型守卫）
- 禁止在循环里做 `JSON.parse` / `JSON.stringify`
- 禁止 20Hz 主循环里分配大量临时对象（用对象池）
- 禁止把配置文件读取放在热路径（启动时加载一次）

---

## 5. 性能红线

| 指标 | 红线 |
|---|---|
| 同屏敌人 | ≤ 40（达上限暂缓投放） |
| 客户端帧率 | PC ≥ 50 FPS |
| 服务端单局 CPU | < 20%（单核，含 44 个 AI 实体） |
| 资源总体积 | < 25 MB |
| 首屏加载 | < 8 秒 |

**AI 计算分帧**：44 个 AI 实体不要在同一 tick 全部决策，按 tick 轮转分批。

**网络优化**：位置用 int16 量化、视野裁剪、差量更新。不要 20Hz 全量广播 float 坐标。

---

## 6. 自测要求

每个里程碑完成时，必须自测通过再报告完成。

```bash
# 部署连通性
curl -I http://127.0.0.1:8080
curl -I http://100.126.150.80:8080
node tools/check-ws.js ws://127.0.0.1:8081/ws

# 类型检查
cd server && npx tsc --noEmit
cd client && npx tsc --noEmit

# 配置校验（每次改 shared/config/*.json 后必跑）
node tools/verify-config.js
# 校验：四波总和恰好 200、五席位、队友约束、武器交叉引用、适龄合规红线
# 退出码 1 时服务端必须拒绝启动
```

**不要在未自测的情况下声称完成。** 说「已完成」意味着你实际运行验证过。

---

## 7. 遇到问题怎么办

| 情况 | 做法 |
|---|---|
| PRD 没覆盖的设计细节 | 按合理默认值实现 + 写进 `OPEN-QUESTIONS.md` 追加区，**不要停下等待** |
| PRD 内部前后矛盾 | 停下来问 watson，不要猜 |
| 技术方案走不通 | 先记录尝试过程，给出替代方案，问 watson 后再改 |
| 需要高风险操作 | 见铁律 10，必须先问 |
| 素材缺失（如地形贴图） | 用纯色/程序化纹理占位，记入待补清单，继续推进 |

---

## 8. 每次交付要说清楚的事

### 必须回写进度

**完成一个里程碑，必须更新 `docs/MILESTONES.md`**：勾选任务清单、改状态图标、填「完成记录」栏。

这不是可选项。`MILESTONES.md` 是进度的唯一记录处，不更新等于没完成。

其中 **M2 的完成记录必须包含实测数据**（队友歼敌占比、平均存活、CPU 占用），只写「已完成」不算数。

### 报告内容

完成一个里程碑时，报告里包含：

1. **做了什么** —— 具体文件和功能
2. **自测结果** —— 实际跑过的命令和输出
3. **偏离 PRD 之处** —— 有没有、为什么
4. **新增的开放问题** —— 写进 OPEN-QUESTIONS 的内容
5. **下一步** —— 下个里程碑的第一个动作

不要写「已完成所有功能」这种没有信息量的总结。

---

## 9. 关键提醒（历史踩坑点）

> 这些是文档评审阶段就识别出的高风险点，务必注意。

| 坑 | 正确做法 |
|---|---|
| 把「先做单人」理解成写本地逻辑 | 单人也连服务器（铁律 3） |
| 把 AI 队友做成跟随玩家的宠物 | 队友是分守路线的协防者，各有防区 |
| AI 队友太强，玩家没有成就感 | M2 实测，队友合计歼敌 ≤ 50%，超了就削 |
| 直接把 1024×1024 原图塞进场景 | 必须走加工管线（见 `skills/asset-pipeline/`） |
| 服务只监听 127.0.0.1 | 必须 `0.0.0.0`，否则 Tailscale 访问不到 |
| WS 地址硬编码 | 必须可配置，从当前域名推导 |
| 四波人数写成 20/30/50 | 是 **30/50/60/60 = 200**，v1.0 版本的旧数字已废弃 |

---

**本文件与 `docs/PRD.md` 配套使用。PRD 说「做什么」，本文件说「怎么做、不许做什么」。**
